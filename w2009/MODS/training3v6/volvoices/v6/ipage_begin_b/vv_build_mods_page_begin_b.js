// vv_build_mods_page_begin_b.js
// placeholder

/* --------------------------------
A separate file for building mods on page begin_b not needed because
vv_build_mods_utility.js contains:

1. global definitions
2. global functions

	a. function replace_apos(str) 
	b. function buildTagsArray_header(downloadTypeN)
	c. function buildTagsArray_closer(downloadTypeN)

All pages use vv_build_mods_utility.js.
   --------------------------------
*/

